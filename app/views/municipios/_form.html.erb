<%= form_with(model: municipio) do |form| %>
  <% if municipio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(municipio.errors.count, "error") %> prohibited this municipio from being saved:</h2>

      <ul>
        <% municipio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :municipio %>
    <%= form.text_field :municipio %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.text_field :estado %>
  </div>

  <div class="field">
    <%= form.label :juris %>
    <%= form.number_field :juris %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
