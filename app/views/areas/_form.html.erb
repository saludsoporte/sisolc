<%= form_with(model: area) do |form| %>
  <% if area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(area.errors.count, "error") %> prohibited this area from being saved:</h2>

      <ul>
        <% area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :area %>
    <%= form.text_field :area %>
  </div>

  <div class="field">
    <%= form.label :estructura_id %>
    <%= form.text_field :estructura_id %>
  </div>

  <div class="field">
    <%= form.label :area_abrev %>
    <%= form.text_field :area_abrev %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
