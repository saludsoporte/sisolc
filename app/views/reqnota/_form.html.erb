<%= form_with(model: reqnotum) do |form| %>
  <% if reqnotum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reqnotum.errors.count, "error") %> prohibited this reqnotum from being saved:</h2>

      <ul>
        <% reqnotum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :requisicion_id %>
    <%= form.text_field :requisicion_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha %>
  </div>

  <div class="field">
    <%= form.label :comenta %>
    <%= form.text_area :comenta %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
