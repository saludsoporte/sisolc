<p>
 <% if current_user %> 
	<%= link_to "Inicio", root_path %> | 
	<% if current_user.rol_id > 0 %>
		<%= link_to "Solicitudes", solicituds_url %> | 
		<%# =TODO link_to "Requisiciones", requisicions_url %> |
	<% end %>
	<%# =TODO link_to "Catalogos", partidas_url %>
  | <%= link_to "Salir del Sistema", logout_path %>
 <% end %>
</p>

<% if [10,15].include?(current_user.rol_id) ||
        (current_user.id == @solicitud.user_id && [1,3].include?(@solicitud.estado_id)) ||
        (current_user.id == @solicitud.vobotecnico_id && @solicitud.estado_id == 2) ||
        @solicitud.adqui_id == current_user.id  ||
        ([5].include?(current_user.rol_id) && [15].include?(current_user.dato2)) %>
        | <%# =TODO link_to 'Cambios datos grales. de Solicitud', edit_solicitud_path(@solicitud) %> |
        <%# =TODO  link_to 'Nuevo articulo', new_articulo_path(:solicitud => @solicitud.id,:partida=>@solicitud.partida_id, :requisicion => @solicitud.requisicion_id, :renglon => @solicitud.renglon_id, :copiar => false) %> |
        <%# =TODO link_to 'Copia articulo', new_articulo_path(:solicitud => @solicitud.id,:partida=>@solicitud.partida_id, :requisicion => @solicitud.requisicion_id, :renglon => @solicitud.renglon_id, :copiar => true) %> |
<% end %>
        | <%# =TODO link_to 'Nuevo comentario', new_solnota_path(:solicitud => @solicitud.id) %>
</p>

<hr />
